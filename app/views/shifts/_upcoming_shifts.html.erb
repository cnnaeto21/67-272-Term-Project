<% if current_user.role?(:admin) || current_user.role?(:manager) %>
    <%= render partial: 'shifts_list', locals: { state: 'Upcoming', 
                                                    shifts: @upcoming_shifts } %>
<%else%>
    <%= render partial: 'shifts3_list', locals: { state: 'Upcoming', 
    shifts: @upcoming_shifts } %>
<%end%>
